<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
    </style>

    <style name="popupTheme" parent="@android:style/Widget.PopupMenu">
        <item name="android:textSize">10sp</item>
    </style>

</resources>
